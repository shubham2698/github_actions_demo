name: Cargo Specific Version

on:
  push:
    branches:
      - main
  workflow_dispatch:


jobs:
  Add-Rust-Version:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Rust 1.72
        run: | 
          curl -y --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
          rustup default 1.72.0
          rustup toolchain install nightly-2023-08-23
          rustup default +nightly-2023-08-24
          cargo --version
          rustc --version


    